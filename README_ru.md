# DSF Splitter

<p align="description">
  <strong>Разделитель DSD-аудио без потерь с использованием CUE-листов</strong><br>
  <sub>Разделите ваши DSF-файлы без потери качества — идеально для создания альбомов с раздельными треками</sub>
</p>

<p align="center">
  <a href="#возможности">Возможности</a> •
  <a href="#установка">Установка</a> •
  <a href="#использование">Использование</a> •
  <a href="#примеры">Примеры</a> •
  <a href="#требования">Требования</a>
</p>

---

## Почему DSF Splitter?

Если вы когда-либо пытались разделить аудиофайл DSF (DSD Stream File), вы знаете эту проблему — большинство инструментов либо:
- ❌ Сначала конвертируют в PCM (потеря качества!)
- ❌ Создают слышимые артефакты в местах разреза
- ❌ Не поддерживают формат DSD вовсе
- ❌ Требуют дорогое профессиональное ПО

**DSF Splitter решает эту проблему** с настоящей **битовой точностью**:
- 🔪 **Разрезает напрямую в DSD-домене** — никакой конвертации в PCM
- 🧬 **Побитовое разделение** — использует интеллектуальный битовый сдвиг для разделений не по границам байтов
- 🎵 **100% оригинальное качество** — каждый бит сохранён

Ваше аудио остаётся **точно таким же**, как оригинал — просто разделённым на отдельные файлы.

## Возможности

- ✅ **Побитовое разрезание в DSD-домене** — разрезает напрямую, никогда не конвертирует в PCM
- ✅ **Без потери качества** — каждый бит сохранён, 100% оригинального аудио
- ✅ **Разделение не по границам байтов** — корректно обрабатывает любые точки разделения с помощью битового сдвига
- ✅ **Поддержка множества треков** — разделяет целые альбомы за одну команду
- ✅ **Сохранение метаданных** — сохраняет названия треков и информацию об исполнителе
- ✅ **Простой CLI** — без GUI, без лишнего функционала
- ✅ **Нет зависимостей** — чистый Python, не требует внешних библиотек

## Установка

### Ручная установка

```bash
git clone https://github.com/ваш-репозиторий/dsf-toolkit.git
cd dsf-toolkit
pip install .
```

### Портативная версия (Без установки)

```bash
python dsf_split.py ваш_файл.cue
```

## Использование

```bash
dsf_split [-h] [-o ВЫВОД] [-f] [-v] cue_файл
```

### Аргументы

| Аргумент | Описание |
|----------|-------------|
| `cue_файл` | Путь к вашему CUE-листу (обязательно) |
| `-o, --output` | Папка вывода (по умолчанию: там же где CUE-файл) |
| `-f, --force` | Перезаписывать существующие файлы |
| `-v, --verbose` | Показывать подробный прогресс |
| `-h, --help` | Показать справку |

## Примеры

### Базовое использование

Разделить альбом:

```bash
dsf_split "Альбом.cue"
```

### Вывод в конкретную папку

```bash
dsf_split "Альбом.cue" -o ./треки
```

### Перезапись существующих файлов

```bash
dsf_split "Альбом.cue" -f
```

### Подробный режим

```bash
dsf_split "Альбом.cue" -v
```

## Как это работает

DSF-файлы используют DSD (Direct Stream Digital) — формат с 1 битом на сэмпл. В отличие от PCM-аудио, вы не можете просто разрезать по границам байтов. Большинство инструментов не справляются, потому что сначала конвертируют в PCM — **DSF Splitter так не делает**.

DSF Splitter работает **полностью в DSD-домене**, используя битовые алгоритмы:
1. Читает оригинальный DSF-файл
2. Деинтерливит многоканальное аудио
3. Извлекает точные **битовые диапазоны** для каждого трека (не сэмплы, не байты — биты!)
4. Создаёт корректно отформатированные DSF-файлы

Результат? **Побитово идентичные** треки без конвертации в PCM, без перекодирования, без потери качества.

## Требования

- Python 3.8+
- Без дополнительных зависимостей

## Поддерживаемые форматы

- **Ввод:** DSF (DSD Stream File), DSD64/128/256
- **Каналы:** Стерео, Многоканальный
- **CUE-лист:** Стандартный формат с INDEX 01

## Устранение неполадок

### "Not a valid DSF file"
Убедитесь, что ваш файл имеет расширение `.dsf` и является корректным DSD Stream File.

### "Only 1-bit DSD supported"
Этот инструмент поддерживает только формат DSD (1-бит), не DST (сжатый DSD).

### Файлы не разделяются
Проверьте, что ваш CUE-лист содержит `INDEX 01` для каждого трека.

## Лицензия

MIT License — см. файл [LICENSE](LICENSE).

---

**Автор:** Шинкаренко Виталий  
**Email:** vitaly@shinkarenko.net

Если этот инструмент сэкономил вам деньги или помог насладиться музыкой, подумайте о поддержке разработки!
